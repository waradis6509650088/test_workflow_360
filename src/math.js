/**
 * * Simple addition.
 * */
const add = (x, y) => x + y;
/**
 * * Simple subtraction.
 * */
const subtract = (x, y) => x - y;
/**
 * * Simple multiplication.
 * */
const multiply = (x, y) => x * y;
module.exports = {
    add,
    subtract,
    multiply,
};
